function ex1(id) {
    let list = []

    function putInt() {
        let num;
        do {
            num = prompt("Escriu un nombre positiu");
            if (isNaN(num)) {
                alert(num + " No es un numero")
            } else {
                list.append
            }
        } while (isNaN(num) || num < 0);
        return list;
    }
    do {

    } while
}